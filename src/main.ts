import { createApp } from 'vue';
import App from './App.vue';

import './styles/main.scss';

// const ExampleElement = defineCustomElement(App);
// customElements.define('weather-app', ExampleElement);

createApp(App).mount('#app');
